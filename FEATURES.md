# 주요 기능 (Key Features)

---

## 1. 3D 갤러리 기반 프로젝트 쇼케이스

- `React Three Fiber`를 활용하여 프로젝트들을 3D 공간 안에서 인터랙티브하게 탐색할 수 있는 갤러리 뷰를 구현하였습니다.
- `@use-gesture/react` 라이브러리를 적용하여 데스크톱 웹뿐만 아니라 모바일 환경에서도 직관적인 스와이프 제스처를 통해 갤러리를 매끄럽게 둘러볼 수 있도록 최적의 UX를 제공합니다.

## 2. RAG 기반 맞춤형 AI 비서 (Portfolio Assistant)

- 초고속 추론을 지원하는 `Groq API`를 활용하여, 사용자의 질문에 포트폴리오 내부 데이터(이력서, 프로젝트 정보 등)를 주입(RAG)해 빠르고 정확하게 답변하는 AI 어시스턴트를 구축하였습니다.
- 단순한 텍스트 답변에 그치지 않고, 대화 문맥을 분석해 사용자가 관심 있어 할 만한 갤러리 뷰나 이력서 화면으로 즉시 이동할 수 있는 네비게이션 버튼을 동적으로 제공합니다.

## 3. Notion 기반의 자동화 업데이트 시스템

- 이력서나 프로젝트 내용을 추가 및 수정할 때 별도의 코드 수정이나 재배포를 거칠 필요 없이, 연결된 Notion 페이지만 편집하면 일정 시간 후 사이트에 자동 반영되도록 파이프라인을 구축했습니다.
- Notion API 응답의 복잡한 계층형 JSON 데이터를 재귀적으로 분석하여 텍스트 포맷, 이미지, 들여쓰기 등 다양한 블록 타입을 프론트엔드 UI 컴포넌트로 완벽하게 매핑하는 커스텀 파서를 직접 구현하였습니다.

## 4. 완벽한 모바일 반응형 레이아웃

- 모바일 디바이스로 포트폴리오에 접속하는 비중이 높은 점을 고려하여, 어떤 해상도에서도 시각적인 깨짐 없이 데스크톱과 동일하게 쾌적한 UI/UX를 경험할 수 있도록 세밀한 반응형 레이아웃을 구현하였습니다.

## 5. FSD(Feature-Sliced Design) 아키텍처 적용

- 효율적인 프로젝트 관리를 위해 모듈형 아키텍처인 FSD를 도입하여, `resume`, `chat`, `projects`, `skills` 등 도메인(Feature)별로 컴포넌트와 비즈니스 로직을 완벽히 분리(`src/features/*`)하였습니다.
- 이를 통해 모듈 간의 결합도를 낮추고 응집도를 높여 프로젝트의 확장성과 향후 유지보수성을 극대화했습니다.
