# Trouble Shooting Log (주요 문제 해결 사례)

이 문서는 포트폴리오 프로젝트를 개발하면서 마주친 기술적 한계를 분석하고, 이를 주도적으로 개선한 핵심 트러블슈팅 경험을 기록한 문서입니다.

---

## 1. 3D 렌더링 최적화: Shadow Map 연산 구조 개선으로 프레임 드랍(FPS) 해결

### 🚨 문제 상황 (Symptom)
* Three.js 기반의 3D 프로젝트 갤러리 뷰(`GalleryScene`)에서 프로젝트 렌더링 개수가 증가할수록, 모바일 및 보급형 데스크톱 기기에서 심각한 프레임 드랍(FPS 저하)과 발열이 발생했습니다.

### 🔍 원인 분석 (Cause)
* **Shadow Map 연산의 O(N) 폭발**:
  기존 구조에서는 각 개별 프로젝트 아이템(`<Frame>`) 컴포넌트 내부에 그림자를 생성하는 `<spotLight castShadow>`가 포함되어 있었습니다.
* WebGL 환경에서 그림자를 생성하는 조명이 늘어날수록 브라우저는 조명 개수만큼 그림자 맵(Shadow Map) 렌더링 패스를 추가로 수행해야 합니다. 즉, N개의 포트폴리오가 존재할 경우 N개의 조명이 작동하여 **렌더링 부하가 O(N)**으로 선형 증가하는 치명적인 병목 지점이었습니다.

### 💡 해결 방법 및 결과 (Solution & Result)
1. **Light 인스턴스 분리 및 단일화 (O(N) ➔ O(1) 복잡도 개선)**
   * 각 `<Frame>` 내부에 하드코딩 되어있던 `<spotLight>`를 제거하고, 최상위 화면인 `<Scene>` 레벨로 조명을 끌어올려 **전체 씬에 단 1개의 그림자 조명만 존재**하도록 아키텍처를 수정했습니다.
2. **동적 타겟팅 및 보간(Interpolation) 애니메이션 적용**
   * 단일 조명이 전체 갤러리를 커버하도록, 조명을 `THREE.Group`에 바인딩하고 현재 포커싱 된 프로젝트 액자 위치(`currentIndex * gap`)를 실시간으로 추적하도록 변경했습니다.
   * 이때 조명이 순간이동하며 부자연스럽게 보이는 것을 방지하기 위해 `maath`의 `easing.damp` 함수를 활용해 카메라의 움직임과 완벽하게 동기화되는 부드러운 위치 보간 애니메이션을 적용했습니다.
3. **결과 (Impact)**
   * 포트폴리오 개수가 몇 개가 추가되더라도 **그림자 맵 연산 복잡도가 항상 O(1)로 고정**됩니다.
   * 시각적인 입체감(조명 효과)은 이전과 동일하게 유지하면서 GPU 메모리 초과 현상을 방지하였고, 중저사양 모바일 기기에서도 안정적인 60FPS를 방어하는 데 성공했습니다.

---

## 2. RAG 기반 AI 어시스턴트 안정성 고도화: 모델 종속성 탈피 및 파싱 방어

### 🚨 문제 상황 (Symptom)
* Groq API의 특정 LLM(`llama-3.3-70b-versatile`)이 권한 이슈 및 Deprecation으로 인해 404 에러를 반환하며 어시스턴트 서비스 전체가 마비되는 단일 장애점(SPOF)이 발생했습니다.
* 대체 모델로 스위칭했을 때, 일부 모델이 API 단의 JSON 강제 출력 옵션(`response_format: { type: "json_object" }`)을 완벽하게 지원하지 않아 예외 에러가 뿜어지고 UI 액션 라우팅이 먹통이 되었습니다.

### 🔍 원인 분석 (Cause)
* 하드코딩된 API 모델 의존성으로 인해 언제든 발생할 수 있는 서비스 장애 상황이었습니다.
* AI가 반환하는 결과물을 파싱할 때 API의 `json_object` 시스템 플래그에만 의존하게 되면, 해당 옵션을 미지원하는 모델로 긴급 교체(Failover) 시 프론트엔드가 응답 구조(Text vs JSON wrapper)를 이해하지 못해 런타임 에러가 발생합니다.

### 💡 해결 방법 및 결과 (Solution & Result)
1. **API 파라미터 의존성 제거 및 프롬프트 엔지니어링 강화**
   * 에러를 유발하는 `response_format` API 파라미터에 의존하는 대신, System Prompt 자체에 강력한 JSON 구조체 예시와 출력 제약 조건을 주입하는 방식으로 RAG 컨텍스트를 고도화했습니다.
2. **자체 텍스트 전처리(Markdown JSON Stripper) 로직 구현**
   * LLM 프롬프트가 강제되었음에도 모델 특성상 결과값에 마크다운 백틱(`````json ... `````)이나 여백을 포함해 뱉더라도, 서버 사이드(Next.js API Route)에서 이를 정규화하여 순수 JSON String만 유효하게 발라내는 자체 방어 로직을 작성했습니다.
3. **결과 (Impact)**
   * 모델 접근 권한 이슈가 터졌을 때 즉각적으로 가용한 서드파티 모델(`openai/gpt-oss-120b` 등)로 유연하게 스위칭할 수 있는 튼튼한 챗봇 기반을 마련했습니다.
   * 프론트엔드 입장에서는 뒷단의 LLM 종류와 상관없이 항상 일관되고 안정적인 `{ text, suggestedAction }` JSON 페이로드를 보장받게 되어, Chat UI의 안정성과 비동기 데이터 처리 경험이 월등하게 향상되었습니다.

---

## 3. Notion API 병목 현상 및 Rate Limit(429) 해결: Promise.all 스태거링(Staggering) 기법 적용

### 🚨 문제 상황 (Symptom)
* Next.js 서버 사이드(SSG) 빌드 및 데이터 프리페치 단계에서, 다수의 포트폴리오 프로젝트 상세 내용(하위 블록)을 Notion API로 한 번에 불러올 때 응답이 실패하거나 페이지 렌더링이 중단되는 문제가 발생했습니다.
* 데이터를 빠르게 가져오기 위해 `for` 반복문(순차적 O(N) 호출) 대신 `Promise.all`을 사용하여 병렬 호출을 시도했으나, Notion 측에서 **429 Too Many Requests (Rate Limited)** 에러를 반환했습니다.

### 🔍 원인 분석 (Cause)
* Notion API는 서버의 과도한 트래픽을 방지하기 위해 엄격한 Rate Limit(초당 평균 3회 요청)을 적용하고 있습니다.
* 무분별하게 `Promise.all`로 다수의 프로젝트 데이터 블록을 비동기로 동시 요청하게 되면서 순식간에 API 허용량을 초과하게 되었습니다. 반면 `await`를 통한 순차 처리는 안전하지만 프로젝트 개수에 비례하여 SSG 빌드 시간을 극단적으로 증가시키는 딜레마가 있었습니다.

### 💡 해결 방법 및 결과 (Solution & Result)
1. **Index 기반의 비동기 지연 (Promise.all Staggering) 적용**
   * 병렬 호출의 이점을 살리되 API 요청이 한꺼번에 몰리지 않도록, 배열의 `index`를 활용하여 일정한 시차를 두고 요청을 분산시켰습니다.
   * `await new Promise((resolve) => setTimeout(resolve, index * 400));` 로직을 추가하여, 각 프로젝트 데이터 요청이 400ms의 시차를 두고 트리거(Stagger) 되도록 병목 현상을 우회했습니다.
2. **트리 노드 깊이(Depth) 딜레이 및 Rate Limit 재시도(Retry) 방어 로직 추가**
   * 프로젝트 내부에 중첩된 노션 블록(들여쓰기, Column 등)을 재귀적으로 파싱할 때에도 초당 한도에 걸릴 수 있습니다. `notion.ts` 내부에서 하위 블록을 가져올 때 `setTimeout`으로 350ms의 미세 딜레이를 추가했습니다.
   * 그럼에도 429 에러가 발생할 경우를 대비해, 예외를 캐치하여 1.5초(1500ms) 대기 후 최대 3회까지 재시도(Retry)하는 Failover 로직을 구현했습니다.
3. **결과 (Impact)**
   * 완전 순차 요청(O(N)) 대비 빌드 및 프리페치 속도를 월등히 단축시키면서도, 429 에러 없이 안정적으로 100% 데이터를 불러올 수 있게 되었습니다.
   * 비동기 처리의 함정에 빠지지 않고, 외부 API의 제약 속에서 성능과 안정성 사이의 최적의 타협점을 찾는 데이터 파이프라인을 구축했습니다.
---

## 4. Notion 이미지 서빙 S3 서명 URL 만료 문제 인프라 우회 해결: Next.js API Proxy & 캐싱 파이프라인 도입

### 🚨 문제 상황 (Symptom)
* 노션 API가 반환하는 프로젝트 썸네일과 프로필 이미지 URL을 그대로 프론트엔드에 렌더링하면, 일정 시간이 지난 뒤 접속한 사용자의 화면에서 엑스박스(깨진 이미지)가 뜨는 현상이 빈번하게 발생했습니다.
* 특히 Three.js의 WebGL 캔버스 기반 3D 환경(\`GalleryScene\`)에서 \`TextureLoader\`를 통해 이미지를 불러올 때 네트워크 블록(403 Forbidden Error) 현상 및 초기 로딩 지연 현상이 두드러졌습니다.

### 🔍 원인 분석 (Cause)
* **S3 Presigned URL 서명 만료**: 노션 API는 내부 데이터베이스 이미지를 Amazon S3에 호스팅하며, 보안을 위해 발급 후 **정확히 1시간(3600초)이 지나면 사용할 수 없는 임시 서명 URL**을 반환합니다.
* 사이트 배포 시점을 기준으로 ISR(Incremental Static Regeneration) 캐시나 빌드 캐시에 1시간짜리 URL 문자가 그대로 정적으로 고정되어 버리기 때문에, 1시간 뒤에 사이트를 방문한 사용자의 브라우저에서는 만료된 주소로 이미지를 요청하게 되어 403 에러가 났던 것입니다.
* 추가적으로, 외부 클라우드 호스트에서 이미지를 3D Canvas 내부 텍스처 데이터로 직접 로드할 때 Cross-Origin(CORS) 보안 제약이 겹치면서 렌더링을 가로막았습니다.

### 💡 해결 방법 및 결과 (Solution & Result)
1. **Next.js 내부 Proxy API 라우트 구축 (인프라 우회)**
   * 클라이언트(웹 브라우저)가 무효화된 노션 S3 주소에 직접 접근하지 않도록, 백엔드 계층에 징검다리 목적의 라우트(\`/api/notion-image-proxy\`)를 직접 건설했습니다.
   * 프론트엔드는 암호화된 내부 경로로 이미지를 통신 요청하고, 서버 측 라우트 객체가 원본 S3 주소로 Buffer 데이터를 \`stream\` 다운로드 받아 곧바로 클라이언트에게 \`Content-Type\`에 맞추어 전달하도록 구조를 우회(Proxy)시켰습니다.
2. **강력한 HTTP 브라우저 캐싱 레이어(\`Cache-Control\`) 적용**
   * Proxy를 거치는 과정에서 생길 수 있는 트래픽 지연을 상쇄하기 위해, 서버에서 응답 헤더에 \`Cache-Control: public, max-age=31536000, immutable\`(1년 보존) 규칙을 강제로 주입시켰습니다. 
3. **결과 (Impact)**
   * 1시간 제한 족쇄가 풀리면서 더 이상 프로젝트 갤러리가 깨져 보이는 에러가 근본적으로 멸종했습니다. 게다가 내부 프록시를 경유하므로 WebGL 이미지 CORS 제약 이슈도 함께 소멸했습니다.
   * 브라우저 계층의 강력한 영구 캐싱 로직이 맞물려, 최초 한 번만 S3을 경유하고 나면 그 다음부터는 사용자의 디바이스 캐시에서 텍스처를 0.1초 만에 즉시 가져오므로 **3D 갤러리 씬의 초기 로딩 및 팝인(Pop-in) 깜박임 현상이 대폭 해결**되었습니다.
