# Technology Stack Document (TECH.md)

이 문서는 인터랙티브 포트폴리오를 기획하고 개발하는 과정에서 채택한 주요 기술 스택의 도입 배경(**Why**)과 실제 적용 방식(**How**)을 담고 있습니다.

---

## 1. AI Assistant & RAG Architecture: Groq API & Next.js API Routes

### Why (도입 배경)
* **LLM의 한계 극복 (RAG 필요성)**: 일반적인 AI 모델은 '김창주'라는 구체적인 개발자의 경력이나 프로젝트 정보를 알지 못합니다. 따라서 포트폴리오 사이트에 방문한 사용자가 던진 질문에 정확히 답하기 위해서는, 사이트가 가진 이력서 및 프로젝트 데이터를 AI에게 주입해 주는 RAG(검색 증강 생성) 아키텍처가 필수적이었습니다.
* **압도적인 추론 응답 속도 (Groq API)**: 챗봇은 일반적인 텍스트 렌더링보다 실시간성이 생명입니다. Groq API는 자체 LPU(Language Processing Unit) 하드웨어를 통해 타 API 대비 압도적으로 빠른 초당 토큰 생성(TPS) 속도를 제공하므로, 지연율(Latency)을 최소화하여 체감 UX를 대폭 끌어올릴 수 있습니다.

### How (활용 방안)
* **프롬프트 컨텍스트 주입 (RAG 구현)**: `src/app/api/chat/route.ts` API 엔드포인트에서 클라이언트로부터 사용자의 질문과 함께 프로젝트(`projects`), 이력서(`resumeData`) 데이터를 JSON으로 전달받고, 이를 시스템 프롬프트(System Prompt)에 컨텍스트로 결합해 모델에게 제공합니다.
* **유연한 모델 호환 및 JSON 마크다운 파싱 처리**: Groq API 환경에서 `llama-3.3` 등 특정 모델의 권한 제약이나 Deprecation 이슈가 발생할 수 있음을 고려해, 현재 권한에서 안정적인 `openai/gpt-oss-120b` 등의 대체 모델을 적용했습니다. 또한 강제 JSON 포맷팅(`response_format`)을 미지원하는 대체 모델들의 특성을 보완하고자 시스템 프롬프트로 JSON 형식을 강제한 후, 반환되는 마크다운 백틱(`````json ... `````)을 자체적으로 정규화(Strip)하고 파싱하는 견고한 방어 로직을 구축했습니다.
* **Suggested Action 포맷 연동**: AI가 단순히 텍스트만 뱉는 것이 아니라 파싱된 JSON에서 `action: navigate` 형태를 반환하면, 프론트엔드(`ChatInterface.tsx`)에서 갤러리 뷰나 이력서 탭 등 적절한 화면으로 사용자를 유도하는 반응형 인터랙션을 구현했습니다.

---

## 2. 3D Interactive Experience: React Three Fiber (R3F) & use-gesture

### Why (도입 배경)
* **사용자 몰입도 ও 체류시간 증대**: 기존의 획일적인 2D 그리드 레이아웃으로는 수많은 포트폴리오들 사이에서 강렬한 인상을 남기기 쉽지 않습니다. 사용자가 직접 화면을 드래그해서 넘겨보는 입체적인 3D 갤러리를 제공하여 시각적 차별화와 호기심을 유도하고자 했습니다.
* **높은 러닝 커브 감소**: WebGL/Three.js 프레임워크를 Vanilla JS로 구현하려면 복잡한 렌더러 설정과 라이프사이클 관리가 필요합니다. React 생태계와 결합된 R3F는 컴포넌트 기반으로 3D Scene을 조작할 수 있게 해 개발 생산성을 높여줍니다.

### How (활용 방안)
* **Drei & Maath 라이브러리 활용**: 3D 액자(`Frame`) 렌더링 시 물리 기반 애니메이션을 부드럽게 처리하는 `easing (maath)`을 사용해, 갤러리 슬라이드 간 카메라 이동을 매끄럽게 처리했습니다.
* **Mobile & Gesture 최적화**: 3D 공간을 터치로 탐색할 수 있도록 `@use-gesture/react`를 적용해 스와이프 이벤트를 캐치하고, 모바일 뷰포트 크기에 따라 `useThree` 훅으로 액자 간격(`gap`) 및 스케일을 자동 조절하는 반응형 3D 환경을 만들었습니다.

---

## 3. Headless CMS: Notion API

### Why (도입 배경)
* **유지보수 효율성 향상**: 포트폴리오에 새로운 프로젝트나 경력이 추가될 때마다 소스 코드를 수정해 커밋/배포하는 것은 비효율적입니다.
* **Notion의 강력한 에디팅 경험**: 이미 개인적으로 관리하고 있는 Notion 데이터베이스를 Headless CMS처럼 활용하면, 익숙한 인터페이스에서 언제든 실시간으로 콘텐츠를 갱신할 수 있습니다.

### How (활용 방안)
* **직접 구축한 블록 파서**: Notion API에서 내려주는 데이터는 계층 구조화된 Block JSON 형태입니다. 이를 자체 파싱 모듈(`src/lib/notion.ts`)을 이용해 텍스트, 이미지, 들여쓰기, 리스트 구조를 프론트엔드에서 렌더링하기 편한 데이터 구조로 재귀적으로 변환했습니다.

---

## 4. Frontend Framework: Next.js (App Router) & Tailwind CSS

### Why (도입 배경)
* **SEO와 FCP(초기 렌더링) 성능 최적화**: 3D 라이브러리와 복잡한 로직이 클라이언트에 몰려 있기 때문에, 정적인 데이터 요소들은 서버에서 미리 가져와 완성된 HTML로 제공해야 초기 로딩 및 검색 엔진 노출 퀄리티를 유지할 수 있습니다. Next.js의 SSG 기능을 활용하기 가장 적합했습니다.
* **빠른 디자인 이터레이션**: 기획 및 개발과 동시에 디자인을 잡아 나갈 수 있도록, 유틸리티 클래스 기반인 Tailwind CSS를 도입하여 스타일 전환(Dark/Light Mode 포함)을 빠르게 처리했습니다.

### How (활용 방안)
* **SSG 최적화**: Notion에서 가져오는 정적 정보들은 빌드 시점(Build Time) 또는 Revalidate 주기에 맞춰 가져오도록 `getStaticProps` 및 서버 컴포넌트 환경에서 Fetch하여 3D 로딩 전까지의 빈약함을 빈틈없이 채워냅니다.
* **Framer Motion 화면 전환**: Next.js의 빠른 페이지 로딩 특성과 결합해, 페이지 마운팅 시 Framer Motion 요소들에 섬세한 딜레이(Delay) 애니메이션을 부여해 끊김없는 시각적 경험을 제공합니다.