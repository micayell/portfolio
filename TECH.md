# 💻 기술 스택 및 핵심 아키텍처 (Tech Stack & Architecture)

본 프로젝트는 불필요한 서브 라이브러리 의존도를 낮추고, **최신 웹 기술의 코어(Core) 자체에 집중**하여 설계되었습니다.

---

## 1. ⚡ 초핵심 기술 스택 (Core Tech Stack)

사용자 경험과 성능을 극대화하기 위해 각 도메인별로 가장 강력하고 효율적인 기술을 채택했습니다.

| 구분 | 핵심 기술 스택 | 설명 |
| --- | --- | --- |
| **Core** | `Next.js 16` (App Router) <br/> `React 19` <br/> `TypeScript` | 최신 렌더링 최적화(RSC, SSR) 및 안정적인 타입 시스템 유지 |
| **Styling** | `Tailwind CSS v4` | 유틸리티 클래스 기반의 직관적이고 빠른 반응형 UI/UX 구현 |
| **3D Engine** | `React Three Fiber` | 브라우저 상에서 부드럽게 구동되는 3D 갤러리 쇼케이스 렌더링 |
| **AI / NLP** | `Groq API` (Llama-3) | 초고속 추론 환경을 이용한 RAG(검색 증강 생성) 기반 맞춤형 AI 비서 |
| **CMS** | `Notion API` | 별도 DB나 어드민 패널 구축 없이 노션을 Headless CMS로 활용 |

---

## 2. 📐 주요 기술적 성과 및 아키텍처 특징 (Key Engineering Highlights)

### 2-1. FSD (Feature-Sliced Design) 아키텍처 전면 적용
단순한 페이지 기반이나 컴포넌트 단위 분리를 넘어, 비즈니스 도메인(`about`, `chat`, `projects`, `resume`, `skills` 등)을 기준으로 디렉토리(`src/features/*`)를 완벽히 분리했습니다.
- 이를 통해 모듈 간 결합도를 최소화하고 응집도를 극대화하여, 향후 사이징이 커지더라도 **확장성과 유지보수성이 흔들리지 않는 구조**를 설계했습니다.

### 2-2. 커스텀 Notion Block 파서(Parser) 직접 구현
단순히 외부 노션 렌더링 라이브러리를 의존한 것이 아니라, **Notion API의 복잡한 계층형 JSON 데이터를 파싱하는 로직(`notion.ts`)을 직접 구축**했습니다.
- 재귀적인(Recursive) 구조로 텍스트 렌더링, 들여쓰기, 이미지 리사이징 등 실무에 가까운 데이터를 DOM(React Node)으로 매핑하여 깊은 수준의 데이터 제어 능력을 확보했습니다.

### 2-3. 상태 관리의 경량화 및 최적화 
`Zustand`나 `React-Query` 같은 전역 상태 / 데이터 패칭 라이브러리를 무분별하게 추가하지 않았습니다.
- 꼭 필요한 부분에서만 Next.js 자체의 API Route와 React Context / Hooks 환경을 활용하여 앱 번들 사이즈를 줄이고 **복잡성을 통제하는 미니멀리즘**을 추구했습니다.

### 2-4. "직접 제어"하는 RAG AI 파이프라인 설계
서드파티 챗봇 위젯을 달아놓은 형태가 아닙니다. `Groq API`를 활용해 직접 백엔드(`route.ts`) 통신 단계를 열고, 사용자 프롬프트에 저의 프로젝트 및 이력(Context) 서빙을 제어했습니다.
- 특히 AI의 답변을 파싱하여, 단순한 응답 텍스트뿐 아니라 `target: "projects"`와 같은 JSON Command를 통해 특정 컴포넌트 탭으로 알아서 이동시키는 **반응형 챗봇 인터페이스**를 구현했습니다.

### 2-5. 프론트엔드 최전선 생태계 검증 수용
안정적인 레거시 버전에 머무르기보다, `Next.js 16` 및 `React 19`, `Tailwind CSS v4` 등과 같은 **시장 최전방에 위치한 생태계 최신 버전**들을 적극 도입했습니다.
- 이를 통해 새로운 프레임워크 패러다임을 발 빠르게 이해하고 적용하는 적응력을 보여줍니다.